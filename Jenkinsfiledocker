pipeline {
   agent any
   stages{
     stage('build'){
       steps{
         sh 'mvn clean package'
         sh "docker build . -t tomcatwebapp:${env.BUILD_ID}"
         sh "docker run -it -p 8080:8080 tomcatwebapp:${env.BUILD_ID}"
        }

      }
    }
  }

